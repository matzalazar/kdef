---
title: semana03
date: '2026-08-31'
tags:
- auto-generado
source: Semana03 - Clase 01 - Parte 3.pptx.pdf
source_path: lenguajes-programacion/08-31-a-09-04/Semana03 - Clase 01 - Parte 3.pptx.pdf
generated_at: '2026-08-31T09:40:17.992463+00:00'
important_dates: []
---

> **Nota:** Este archivo fue generado automáticamente por el pipeline de kdef.
> El contenido proviene de materiales del aula virtual de la UNDEF.
> Para correcciones, abrí un issue en el repositorio.

# Semana 03 - Clase 01 - Parte 3

## Conceptos clave  
- **Operador**: símbolo que realiza operaciones con datos.  
- **Operadores aritméticos**: `+`, `-`, `*`, `/`, `%` (binarios); `++`, `--` (unarios) con diferencia entre prefijo (`++a`) y postfijo (`a++`).  
- **Operadores de comparación**: `==`, `!=`, `>`, `<`, `>=`, `<=`.  
- **Operadores lógicos**: `!` (NOT), `&&` (AND), `||` (OR).  
- **Precedencia y asociatividad**: unarios `++ --` > `* / %` > `+ -` > `=`; asociatividad izquierda a derecha excepto unarios y asignación (derecha a izquierda).  
- **Estructuras de control**: `if/else`, `switch/case`, bucles `for` (exacto), `while` (0 a n), `do-while` (1 a n).  
- **OnlineGDB**: IDE en la nube que permite editar, compilar y ejecutar programas C/C++ con un solo clic (https://www.onlinegdb.com/).  

## Desarrollo principal  
El documento comienza definiendo los operadores en C y los clasifica en aritméticos, de comparación, lógicos y de asignación compuesta, mostrando ejemplos y aclarando la diferencia entre los operadores unarios prefijo y postfijo. Luego explica el orden de precedencia y la asociatividad al evaluar expresiones complejas. A continuación se presentan las estructuras de decisión: la sentencia `if/else` (incluyendo el error común de confundir `=` con `==`) y la sentencia `switch/case` (con énfasis en el uso de `break` para evitar el efecto cascada y en `default` para cubrir casos no contemplados). Finalmente se describen los bucles: `for` (cuando se conoce el número de iteraciones), `while` (condición evaluada antes de entrar, posible cero ejecuciones) y `do-while` (condición evaluada al final, garantizando al menos una ejecución). Se menciona la herramienta OnlineGDB como recurso para practicar código en línea y se concluye con un repaso de lo aprendido y el anuncio del próximo tema (algoritmos básicos, vectores y matrices).  

## Conclusiones  
Se afianzó el manejo de los operadores y su precedencia, fundamental para escribir expresiones correctas en C. Se comprendieron las estructuras de control que permiten dirigir el flujo del programa según condiciones y repeticiones, y se conoció un entorno práctico (OnlineGDB) para compilar y ejecutar código sin necesidad de instalar un entorno local.
