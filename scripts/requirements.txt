# kdef pipeline — dependencias Python
# Instalar con: pip install -r scripts/requirements.txt

# Reintentos con backoff exponencial para llamadas a LLM
tenacity>=9.1.4

# Google Gemini — LLM fallback
google-genai>=2.19.0

# OpenAI SDK — usado también para OpenRouter (API compatible)
openai>=3.3.1

# Variables de entorno desde .env en desarrollo local
python-dotenv>=1.2.3

# HTTP client para scraping de Moodle
requests>=2.34.2

# Parsing de HTML para scraping de Moodle
beautifulsoup4>=4.15.0
lxml>=6.1.2

# YAML para el catálogo declarativo del campus
PyYAML>=6.0.3

# Extracción de texto de PDFs
pypdf>=6.16.1

# Extracción de texto de documentos Office
python-docx>=1.2.0
python-pptx>=1.0.2
